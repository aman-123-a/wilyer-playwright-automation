# tests/cms4 — owned by cms4.pocsample.in

Feature under development here: **file conversion**
(`file-conversion` in [`config/features.ts`](../../config/features.ts)).

No automated suite has been authored yet. New specs go in
`tests/cms4/file-conversion/<test-type>/`, importing shared code four levels up
(`../../../../fixtures/test-fixtures`).

Run them with `npm run cms4`. See [../README.md](../README.md) for how folder
ownership and rollout work.
